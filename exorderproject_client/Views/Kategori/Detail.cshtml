
@{
    ViewBag.Title = "Detail";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<header class="page-header">
    <h2>KATEGORİ DETAY</h2>
    <div class="right-wrapper text-right pr-3 pt-2">
        <a href="/kategori/liste" class="btn btn-danger btn-sm text-light"><i class="fa fa-chevron-left mr-2"></i>Kategori Listesi</a>
    </div>
</header>
<div class="row">
    <div class="col-lg-12 mb-3">
        <section class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-12">
                        <div class="tabs tabs-primary">
                            <ul class="nav nav-tabs">
                                <li class="nav-item active">
                                    <a class="nav-link tabMenuItem" href="#genel" data-toggle="tab" data-url="@Url.Action("_Genel","Kategori", new { id = ViewBag.ID})">Genel Bilgiler</a>
                                </li>
                                
                            </ul>
                            <div class="tab-content">
                                <div id="tabContent" class="tab-pane active">
                                    @Html.Action("_Genel", "Kategori")
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
</div>

