@model List<exorderproject_core.DTO.ALTBOLGE>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<header class="page-header">
    <h2>ALT BÖLGE</h2>
    <div class="right-wrapper text-right pr-3 pt-2">
        <a href="/altbolge/yeni" class="btn btn-primary btn-sm"><i class="fa fa-plus mr-2"></i>Yeni Alt Bölge</a>
    </div>
</header>
<div class="row">
    <div class="col-lg-12 mb-3">
        <section class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-12">
                        <table class="table table-bordered table-hover table-striped table-sm mb-0" id="datatable-tabletools">
                            <thead>
                                <tr>
                                    <th>
                                        @Html.DisplayName("Durum")
                                    </th>
                                    <th>
                                        @Html.DisplayName("Bölge")
                                    </th>
                                    <th>
                                        @Html.DisplayName("Açıklama")
                                    </th>
                                    
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                <tr onclick="document.location = '/altbolge/detay/@item.REGIONS_ID'" style="cursor: pointer">
                                    <td>
                                        @Html.DisplayFor(modelItem=>item.REGIONS_STATUS_NAME)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.REGIONS_NAME)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.REGIONS_DESCRIPTION)
                                    </td>
                                    
                                </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </section>
    </div>
</div>

