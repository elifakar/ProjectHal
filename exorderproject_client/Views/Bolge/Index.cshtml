@model List<exorderproject_core.DTO.BOLGE>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<header class="page-header">
    <h2>BÖLGE</h2>
    <div class="right-wrapper text-right pr-3 pt-2">
        <a href="/bolge/yeni" class="btn btn-primary btn-sm"><i class="fa fa-plus mr-2"></i>Yeni Bölge</a>
    </div>
</header>
<div class="row">
    <div class="col-lg-12 mb-3">
        <section class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-12">
                        <table class="table table-bordered table-hover table-striped table-sm mb-0" id="datatable-tabletools">
                            <thead>
                                <tr>
                                    <th>
                                        @Html.DisplayName("Durum")
                                    </th>
                                    <th>
                                        @Html.DisplayName("Bölge")
                                    </th>
                                    <th>
                                        @Html.DisplayName("Açıklama")
                                    </th>
                                    
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                <tr onclick="document.location = '/bolge/detay/@item.REGION_ID'" style="cursor: pointer">
                                    <td>
                                        @Html.DisplayFor(modelItem=>item.REGION_STATUS_NAME)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.REGION_NAME)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.REGION_DESCRIPTION)
                                    </td>
                                    
                                </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </section>
    </div>
</div>

