@{
    ViewBag.Title = "Detail";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<header class="page-header">
    <h2>PROFİL</h2>
</header>
<div class="row">
    <div class="col-lg-12 mb-3">
        <section class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-12">
                        <div class="tabs tabs-primary">
                            <ul class="nav nav-tabs">
                                <li class="nav-item">
                                    <a class="nav-link tabMenuItem active" href="#firmaayarlari" data-toggle="tab" data-url="@Url.Action("_FirmaGuncelle","Firma")">Firma Ayarları</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link tabMenuItem" href="#sifreayarlari" data-toggle="tab" data-url="@Url.Action("_SifreGuncelle", "Kullanici")">Şifre Ayarları</a>
                                </li>
                            </ul>
                            <div class="tab-content">
                                <div id="tabContent" class="tab-pane active">
                                    @Html.Action("_FirmaGuncelle", "Firma")
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
</div>
